version: 1
name: cargo generic dependency review
repeats: 2
workdir: /workspace
network:
  mode: default
telemetry:
  mode: required
success:
  exit_code: 0
steps:
  - name: install dependency
    run: install
